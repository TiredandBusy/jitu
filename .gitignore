#include<iostream>
using namespace std;

int main()
{

	int n, m;
	cin >> n >> m;
	int a = (m - 2 * n) / 2;
	int b = n - a;
	if (m % 2 == 1 || a < 0 || b < 0)
		cout << "no" << endl;
	else
		cout << b << " " << a << endl;
	system("pause");
	return 0;
}
